<p id="notice"><%= notice %></p>



 <div id="itemsContainer">
   <h1><%= @item.title %></h1>
   <div class="imageBig">
   <%= image_tag @item.image_url, :class => 'welcomIm'  %>
   </div>
       <h2><%= @item.description %></h1>
   
    <div class="itemholsshow">
       <p>Price <%= number_to_currency(@item.price, :unit => "€") %>
       <br>
        Category: <%= @item.category %>
        <br>
        Brand: <%= @item.brand %></p>
       
       <p>
         <%= link_to 'Back',  items_path, :class => "button", :role => "button" %>
          <% if user_signed_in? && current_user.admin? %>
                <%= link_to 'Edit',  edit_item_path(@item), :class => "button", :role => "button" %>
          <% end %>

       </p>
        
      </div>
   
   
   </div>